git init

git status

git add .

git commit -m "Added terraform-vpc-handson project"

git remote add origin https://github.com/VasundharaDPavuluri/terraform-labs.git

git remote -v

git push -u origin master

After new changes:
git add .

git commit -m "Updated VPC project"

git push
